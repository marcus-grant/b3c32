# Unplanned

Idea intake sorted by maturity tier.
P1 is nearest to promotion into roadmap or TODO,
P4 is a parked thought.
Promoted entries are deleted, not marked promoted.

## P2

- Convenience vector cap policy:
  cap at four,
  additions dilute the file's mostly-certified character,
  formalize the cap in the conformance doc

## P3

- Draft erratum submission:
  a counting error found in a table
  near the end of the governing Crockford draft,
  submit upstream,
  note in conformance which published rows it touches if any

## P4

- Multi-hasher cohabitation:
  if scope ever expands past blake3,
  b3c32 gets renamed and hashers get their own subdirectories,
  far beyond current scope
